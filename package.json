{
  "name": "typesafe-jev-starter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npm run build && node dist/src/index.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test dist/test/**/*.test.js",
    "start": "node dist/src/index.js",
    "eval": "npm run build && node dist/evaluation/run.js",
    "eval:ko": "npm run build && node dist/evaluation/run.js --set=ko",
    "eval:ko-cases": "npm run build && node dist/evaluation/run.js --set=ko --lang=ko"
  },
  "dependencies": {
    "@typesafe-ai/sdk": "0.6.0",
    "dotenv": "^17.2.2"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2"
  }
}
